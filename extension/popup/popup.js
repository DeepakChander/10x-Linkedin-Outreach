function update(){chrome.runtime.sendMessage({type:'10X_GET_STATUS'},r=>{if(!r)return;const s=document.getElementById('status');s.textContent=r.connected?'Connected':'Disconnected';s.className='status '+(r.connected?'connected':'disconnected');if(r.counts){document.getElementById('connections').textContent=r.counts.connections||0;document.getElementById('messages').textContent=r.counts.messages||0;document.getElementById('inmails').textContent=r.counts.inmails||0;document.getElementById('scrapes').textContent=r.counts.scrapes||0}})}update();setInterval(update,2000);document.getElementById('reconnect').addEventListener('click',update);